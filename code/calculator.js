function add(a, b) {
  return a + b;
}

function subtract(x, y) {
  return x - y;
}

function multiply(c, d) {
  return c * d;
}

function divide(e, f) {
  return e / f;
}

module.exports = {
  add: add,
  subtract: subtract,
  multiply: multiply,
  divide: divide,
};
